class Position { //used to define the position of different objects, so that there won't be too many position variable of the objects e.g. mxpos, mypos
  int x, y;
  
  Position(int posX, int posY) {
    x = posX;
    y = posY;
  }
}